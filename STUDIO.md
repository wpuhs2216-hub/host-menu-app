# メニュー表アプリ（初回案内）

## このソフトは何か

ホストクラブの店内タブレットで使う「メニュー表」アプリです。
お客様にキャストの写真と料金表を見せ、気になった人にチェックを入れて送信すると、
スタッフ側に「どの席で誰が選ばれたか」が届きます。
ご新規様の同意書に手書きで署名してもらう機能も入っています。

店舗は 8 つ（動作確認用の「テスト店舗」を含む）。起動時に店舗パスワードを入れると、
その端末はその店に固定されます。店舗の一覧は `src/storeContext.js` の `STORES`。

## 土台

- 作り: Vanilla JS + Vite。Android アプリ化は Capacitor
- 画面: `index.html`（メニュー）/ `admin.html`（管理画面）
- クラウド: Supabase（Postgres + Storage + Realtime）
- 画像の本体: 端末の IndexedDB とクラウドの `panel-images` バケット
- 配布: Android は GitHub Releases の APK（`apk.html` が常に最新へ転送）、
  ブラウザ版は GitHub Pages（`https://wpuhs2216-hub.github.io/host-menu-app/`）
- 練習用の環境: `supabase start`（手順は `docs/local-test-env.md`）。検証は必ずテスト店舗で行う

## データの置き場

クラウド（Supabase）— 定義は `db/schema.sql`

| 印 | 何が入るか |
|---|---|
| 札 | メニューに出す1枚1枚（料金表の画像・キャストの写真）。店ごとに分かれる |
| 指名履歴 | どの席でどのキャストが選ばれたかの記録。店ごとに分かれる |
| 選択中 | いまチェックが付いているキャストと色。確定すると消える一時置き場 |
| 店の設定 | 店ごとの卓番・色ラベル・書体・台の既定設定。全台で共通 |
| 同意書 | ご新規様の手書き署名と記入内容。追記だけで消さない（個人情報あり） |

端末の中（localStorage / IndexedDB）— 定義は `src/store.js` ほか

| 印 | 何が入るか |
|---|---|
| 札の控え | クラウドの札をこの端末に写した物 |
| 指名履歴の控え | この端末で出した指名の記録 |
| 台の設定 | この端末だけの設定（文字の大きさ・鍵・テスト用） |
| 台の上書き | 店の既定に逆らってこの台だけ変えた設定 |
| 上書きの引っ越し済み印 | 昔の設定を上書きへ移し終えたかどうか |
| 店の設定の控え | クラウドの店の設定をこの端末に写した物 |

## 設定の決まり方（2026-09-25 から）

運用に関わる設定は二段構えです。上から順に見て、最初に見つかった値が効きます。

1. 台の上書き（この端末だけ オン／オフ）
2. 店の既定（管理画面の「店舗設定」で決める。全台に降りる）
3. プログラムの既定値

どの設定が二段構えかは `src/store.js` の `SHARED_SETTINGS` に書いてあります。
端末名・パターンロック・署名のテストモード・文字の大きさは、台ごとのままです。

## まだやっていないこと

- 料金システム／ALLCAST の振り分けは「源氏名が空かどうか」で自動。1枚ずつ手で移す仕組みは無い
- 店の既定を変えた時、他の端末に届くのはその端末がアプリを開き直した時（即時ではない）
- `supabase/migrations/` と `db/*.sql` は手で同じ内容を並べている（自動で揃える仕組みは無い）
